# Build outputs
build/
out/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
*.dll
*.lib
*.exe
*.pdb
*.obj
*.o
*.a

# IDE
.vs/
.vscode/
*.suo
*.user
*.sln
*.vcxproj*
.idea/

# Generated files
CMakePresets.json
*_pb2.py
*.pb.cc
*.pb.h
compile_commands.json
# Build-generated version placeholder in the vendored morphizen subtree
morphizen/morphizen-core/version.txt

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
*.egg
.venv/

# Test outputs
*.onnx
!backend-mlir-compiler/test/models/*.onnx
!examples/*.onnx
*.dat
test_output/

# Numeric test suite (test/numeric/)
#   python_test_output/    -- single root for all runtime output:
#       intermediate/      per-test ephemeral artefacts (model + inputs + actual/expected outputs);
#                          deleted on pass, retained on fail for postmortem
#       cache/             persistent reference-output cache, keyed by sanitised pytest node id
#   .pytest_cache/         pytest's own metadata (--lf last-failed, etc.); also self-ignores
#   run_numeric_tests.bat  per-developer convenience wrapper; not part of the suite
#                          (each contributor wraps the README example to suit their layout)
test/numeric/python_test_output/
test/numeric/.pytest_cache/
test/numeric/run_numeric_tests.bat

# OS
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log

# Cline/AI assistant storage
.cline_storage/
.cline/
# Cursor project config (local IDE state). Ignore everything under .cursor/*,
# then re-include rules, commands, and skills (never bare .cursor/ — git cannot
# un-ignore children of a fully ignored directory; Cursor also skips gitignored
# trees for slash commands and skills).
.cursor/*
!.cursor/
!.cursor/rules/
!.cursor/rules/**
!.cursor/commands/
!.cursor/commands/**
!.cursor/skills/
!.cursor/skills/**
.cursor/skills/**/__pycache__/
.cursor/skills/**/*.pyc

# Claude Code workspace (worktrees, local settings, sdd ledger ΓÇö never committed)
.claude/

# Agentic spec/plan scratch docs (superpowers) ΓÇö working artifacts, not shipped
docs/superpowers/

# Additional IDE/editor files
*.sublime-project
*.sublime-workspace
.project
.cproject
.settings/

# Temporary files
*.tmp
*.temp
*.bak
*.cache

# Dependency directories (if any)
node_modules/
vendor/

# Coverage reports
coverage/
*.gcov
*.gcda
*.gcno

# Static analysis
.clang-tidy
.clang-format
.cache/

# Security - don't commit credentials
*.pem
*.key
*.crt
.env
.env.*

# MLIR debugging artifacts (use /c/temp/ instead)
backend-mlir-compiler/test/*.mlir
backend-mlir-compiler/test/ir_dumps/

# LIT test timing cache (generated by lit runner)
.lit_test_times.txt
model.constants.bin
model.constants.json

# Large model files (keep models out of git)
full_model/

# Local analysis/benchmark artifacts
Llama_Sliced/
perf_seq*.txt
dump_log.txt

# Local build scripts (contain machine-specific paths)
setup_and_configure.ps1
build_and_install.ps1

# build.py install directory
install/

# ONNX model files (large, downloaded on demand)
models/
_local/

# Whisper test audio (auto-downloaded at test setup, cached locally; README kept)
test/python/data/whisper/*.wav
test/python/data/whisper/librispeech/

.idea/
.depend/
CMakeUserPresets.json
build.*/

# Jekyll documentation site (site/): local build output and gem cache. The
# published site is built by .github/workflows/pages.yml, never committed.
# The destination is the repository root's _site/ because that is where the
# workflow writes it, so a local build and CI produce identical layouts.
/_site/
site/.jekyll-cache/
site/.jekyll-metadata
site/vendor/
site/.bundle/
