##
## Copyright (C) 2026 Advanced Micro Devices, Inc. All rights reserved.
## Licensed under the MIT License.
##

# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-added-large-files
    -   id: fix-byte-order-marker
    -   id: mixed-line-ending
    -   id: check-merge-conflict
-   repo: local
    hooks:
    -   id: lintrunner
        name: lintrunner
        entry: lintrunner
        language: python
        additional_dependencies:
          - lintrunner==0.12.7
          - lintrunner-adapters==0.12.4
          - ruff==0.11.9
          - clang-format==16.0.1
        types: [file]
        args: [--apply-patches]
        require_serial: true
    -   id: licenseheaders
        name: licenseheaders
        entry: python -m licenseheaders
        # -x excludes paths from license-header insertion. morphizen/* (the
        # vendored subtree) keeps its own upstream copyright headers, and
        # lib/Runtime/Kernels/* (the hand-written GPU kernels) keeps its existing
        # headers untouched. site/* is the Jekyll documentation site: Jekyll
        # requires the "---" front matter to be the very first line of a file,
        # so a prepended comment header would silently demote every page to an
        # unrendered static asset. Its headers are carried inside the front
        # matter or in a Liquid comment instead.
        args: [-t,./LICENSES/license.txt,-x,morphizen/*,lib/Runtime/Kernels/*,site/*,-s,./LICENSES/settings.json,-f]
        language: python
        additional_dependencies: [licenseheaders==0.8.8]
        types: [file]
